<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ruben L Almodiel | IT Specialist & Developer</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #87CEEB;
            --primary-dark: #5DADEC;
            --secondary: #4682B4;
            --accent: #00BFFF;
            --dark: #0C2D48;
            --light: #F0F8FF;
            --gray: #B0C4DE;
            --gray-light: #E6F3FF;
            --gray-medium: #7CB9E8;
            --white: #FFFFFF;
            --black: #1E3A5F;
            --gradient: linear-gradient(135deg, #87CEEB 0%, #4682B4 100%);
            --gradient-accent: linear-gradient(135deg, #00BFFF 0%, #1E90FF 100%);
            --gradient-light: linear-gradient(135deg, #E6F3FF 0%, #F0F8FF 100%);
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        html {
            scroll-behavior: smooth;
            overflow-x: hidden;
        }
        
        body {
            font-family: 'Poppins', sans-serif;
            line-height: 1.6;
or: var(--black);
            background-color: var(--light);
            overflow-x: hidden;
            background: linear-gradient(135deg, #E6F3FF 0%, #F0F8FF 100%);
            min-height: 100vh;
            position: relative;
        }

        /* Animated background elements */
        .bg-animation {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            overflow: hidden;
        }

        .bg-circle {
            position: absolute;
            border-radius: 50%;
            background: radial-gradient(circle, rgba(135, 206, 235, 0.1) 0%, transparent 70%);
            animation: float 20s infinite linear;
        }

        .bg-circle:nth-child(1) {
            width: 300px;
            height: 300px;
            top: 10%;
            left: 5%;
            animation-delay: 0s;
            animation-duration: 25s;
        }

        .bg-circle:nth-child(2) {
            width: 200px;
            height: 200px;
            top: 60%;
            right: 10%;
            animation-delay: 5s;
            animation-duration: 20s;
            background: radial-gradient(circle, rgba(0, 191, 255, 0.1) 0%, transparent 70%);
        }

        .bg-circle:nth-child(3) {
            width: 150px;
            height: 150px;
            bottom: 20%;
            left: 20%;
            animation-delay: 10s;
            animation-duration: 15s;
            background: radial-gradient(circle, rgba(70, 130, 180, 0.1) 0%, transparent 70%);
        }

        @keyframes float {
            0%, 100% {
                transform: translate(0, 0) rotate(0deg);
            }
            25% {
                transform: translate(20px, 20px) rotate(90deg);
            }
            50% {
                transform: translate(0, 40px) rotate(180deg);
            }
            75% {
                transform: translate(-20px, 20px) rotate(270deg);
            }
        }
        
        .container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        
        /* Header & Navigation with Glass Morphism */
        header {
            background: rgba(12, 45, 72, 0.9);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(135, 206, 235, 0.2);
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.1);
        }

        header.scrolled {
            padding: 5px 0;
            background: rgba(12, 45, 72, 0.95);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }
        
        .nav-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 0;
            transition: all 0.3s ease;
        }

        header.scrolled .nav-container {
            padding: 10px 0;
        }
        
        .logo {
            font-family: 'Montserrat', sans-serif;
            font-size: 1.8rem;
            font-weight: 700;
            color: var(--white);
            position: relative;
            overflow: hidden;
        }

        .logo::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 2px;
            background: var(--primary);
            transform: translateX(-100%);
            transition: transform 0.6s cubic-bezier(0.215, 0.610, 0.355, 1);
        }

        .logo:hover::after {
            transform: translateX(0);
        }
        
        .nav-links {
            display: flex;
            gap: 30px;
        }
        
        .nav-links a {
            text-decoration: none;
            color: var(--light);
            font-weight: 500;
            position: relative;
            padding: 5px 0;
            transition: all 0.3s ease;
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 2px;
            background: var(--primary);
            transform: scaleX(0);
            transform-origin: right;
            transition: transform 0.4s cubic-bezier(0.215, 0.610, 0.355, 1);
        }

        .nav-links a:hover {
            color: var(--primary);
        }

        .nav-links a:hover::after {
            transform: scaleX(1);
            transform-origin: left;
        }
        
        .mobile-menu-btn {
            display: none;
            font-size: 1.5rem;
            cursor: pointer;
            background: var(--gradient);
            width: 40px;
            height: 40px;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
            color: white;
            border: 1px solid rgba(135, 206, 235, 0.3);
        }

        .mobile-menu-btn:hover {
            transform: rotate(90deg);
            background: var(--gradient-accent);
        }
        
        /* Hero Section with Parallax Effect */
        .hero {
            padding: 180px 0 100px;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at 30% 20%, rgba(135, 206, 235, 0.1) 0%, transparent 50%),
                        radial-gradient(circle at 70% 80%, rgba(0, 191, 255, 0.1) 0%, transparent 50%);
            z-index: -1;
        }
        
        .hero-content {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 50px;
        }
        
        .hero-text {
            flex: 1;
            transform: translateY(30px);
            opacity: 0;
            animation: fadeUp 1s cubic-bezier(0.215, 0.610, 0.355, 1) forwards;
            animation-delay: 0.3s;
        }
        
        @keyframes fadeUp {
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }
        
        .hero-text h1 {
            font-family: 'Montserrat', sans-serif;
            font-size: 3.5rem;
            line-height: 1.1;
            margin-bottom: 10px;
            color: var(--black);
            position: relative;
            display: inline-block;
        }

        .hero-text h1::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 0;
            width: 100px;
            height: 4px;
            background: var(--primary);
            border-radius: 2px;
        }

        /* Profile Image Container */
        .profile-image-container {
            display: flex;
            align-items: center;
            margin: 25px 0 20px;
            gap: 20px;
        }

        .profile-img-wrapper {
            position: relative;
            width: 120px;
            height: 120px;
            border-radius: 50%;
            overflow: hidden;
            border: 3px solid var(--primary);
            padding: 3px;
            box-shadow: 0 10px 30px rgba(135, 206, 235, 0.3);
            animation: floatProfile 6s ease-in-out infinite;
        }

        @keyframes floatProfile {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-10px);
            }
        }

        .profile-img {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }

        .profile-img-wrapper:hover .profile-img {
            transform: scale(1.1);
        }

        .profile-img-wrapper::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(135, 206, 235, 0.2);
            opacity: 0;
            border-radius: 50%;
            transition: opacity 0.5s ease;
            z-index: 1;
        }

        .profile-img-wrapper:hover::before {
            opacity: 0.3;
        }

        .profile-info {
            flex: 1;
        }

        .profile-name {
            font-size: 1.8rem;
            font-weight: 700;
            color: var(--black);
            margin-bottom: 5px;
        }

        .profile-title {
            font-size: 1.1rem;
            color: var(--secondary);
            margin-bottom: 10px;
        }

        .profile-tags {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            margin-top: 10px;
        }

        .profile-tag {
            background: rgba(135, 206, 235, 0.2);
            color: var(--secondary);
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 0.85rem;
            border: 1px solid rgba(135, 206, 235, 0.3);
        }
        
        .hero-text .title {
            font-size: 1.3rem;
            color: var(--secondary);
            margin-bottom: 15px;
            display: inline-block;
            padding: 5px 15px;
            background: rgba(135, 206, 235, 0.2);
            border-radius: 20px;
            border: 1px solid rgba(135, 206, 235, 0.3);
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% {
                box-shadow: 0 0 0 0 rgba(135, 206, 235, 0.4);
            }
            70% {
                box-shadow: 0 0 0 10px rgba(135, 206, 235, 0);
            }
        }
        
        .hero-text p {
            font-size: 1.2rem;
            color: var(--black);
            opacity: 0.8;
            margin-bottom: 30px;
            max-width: 600px;
        }
        
        .hero-image {
            flex: 1;
            display: flex;
            justify-content: center;
            transform: translateX(30px) rotate(-5deg);
            opacity: 0;
            animation: slideInRight 1s cubic-bezier(0.215, 0.610, 0.355, 1) forwards;
            animation-delay: 0.6s;
        }

        @keyframes slideInRight {
            to {
                transform: translateX(0) rotate(0);
                opacity: 1;
            }
        }
        
        .profile-container {
            position: relative;
            width: 350px;
            height: 350px;
            perspective: 1000px;
        }

        .profile-card {
            width: 100%;
            height: 100%;
            position: relative;
            transform-style: preserve-3d;
            transition: transform 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .profile-container:hover .profile-card {
            transform: rotateY(180deg);
        }

        .profile-front, .profile-back {
            position: absolute;
            width: 100%;
            height: 100%;
            backface-visibility: hidden;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 20px 40px rgba(135, 206, 235, 0.2);
        }

        .profile-front {
            background: var(--gradient);
            display: flex;
            align-items: center;
            justify-content: center;
            border: 1px solid rgba(135, 206, 235, 0.3);
        }

        .profile-front i {
            font-size: 10rem;
            color: white;
            filter: drop-shadow(0 5px 15px rgba(0, 0, 0, 0.1));
        }

        .profile-back {
            background: rgba(255, 255, 255, 0.95);
            transform: rotateY(180deg);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 30px;
            text-align: center;
            border: 1px solid rgba(135, 206, 235, 0.2);
        }

        .profile-back h3 {
            font-size: 1.8rem;
            margin-bottom: 10px;
            color: var(--black);
        }

        .profile-back p {
            color: var(--black);
            opacity: 0.8;
            margin-bottom: 20px;
        }

        .flip-hint {
            position: absolute;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            color: var(--white);
            font-size: 0.9rem;
            background: rgba(12, 45, 72, 0.7);
            padding: 5px 15px;
            border-radius: 20px;
            animation: bounce 2s infinite;
            border: 1px solid rgba(135, 206, 235, 0.3);
        }

        @keyframes bounce {
            0%, 100% {
                transform: translateX(-50%) translateY(0);
            }
            50% {
                transform: translateX(-50%) translateY(-5px);
            }
        }
        
        .btn {
            display: inline-block;
            padding: 14px 32px;
            background: var(--gradient);
            color: white;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            border: none;
            cursor: pointer;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            box-shadow: 0 10px 20px rgba(135, 206, 235, 0.3);
            position: relative;
            overflow: hidden;
            z-index: 1;
            border: 1px solid rgba(135, 206, 235, 0.3);
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: var(--gradient-accent);
            transition: left 0.6s cubic-bezier(0.215, 0.610, 0.355, 1);
            z-index: -1;
        }

        .btn:hover {
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 15px 30px rgba(135, 206, 235, 0.4);
            color: white;
        }

        .btn:hover::before {
            left: 0;
        }
        
        /* Achievements Section with 3D Cards */
        .achievements {
            padding: 100px 0;
        }
        
        .section-title {
            text-align: center;
            font-family: 'Montserrat', sans-serif;
            font-size: 2.8rem;
            margin-bottom: 60px;
            position: relative;
            color: var(--black);
            opacity: 0;
            transform: translateY(30px);
            animation: fadeUp 1s forwards;
            animation-delay: 0.2s;
        }

        .section-title::after {
            content: '';
            position: absolute;
            width: 80px;
            height: 4px;
            background: var(--primary);
            bottom: -15px;
            left: 50%;
            transform: translateX(-50%);
            border-radius: 2px;
        }
        
        .achievement-cards {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 30px;
        }
        
        .achievement-card {
            background: rgba(255, 255, 255, 0.8);
            border-radius: 20px;
            padding: 35px;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(135, 206, 235, 0.3);
            transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            transform-style: preserve-3d;
            transform: perspective(1000px) rotateY(0deg);
            opacity: 0;
            transform: translateY(50px) rotateX(10deg);
        }

        .achievement-card.animate {
            animation: cardAppear 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards;
        }

        @keyframes cardAppear {
            to {
                opacity: 1;
                transform: translateY(0) rotateX(0);
            }
        }

        .achievement-card:hover {
            transform: translateY(-15px) scale(1.03);
            box-shadow: 0 25px 50px rgba(135, 206, 235, 0.2);
            border-color: rgba(135, 206, 235, 0.5);
            background: rgba(255, 255, 255, 0.95);
        }
        
        .achievement-icon {
            font-size: 3rem;
            margin-bottom: 20px;
            display: inline-block;
            padding: 15px;
            border-radius: 15px;
            background: var(--gradient);
            color: white;
            box-shadow: 0 10px 20px rgba(135, 206, 235, 0.3);
            transform: rotate(0deg);
            transition: all 0.5s ease;
            border: 1px solid rgba(135, 206, 235, 0.3);
        }

        .achievement-card:hover .achievement-icon {
            transform: rotate(15deg) scale(1.1);
            background: var(--gradient-accent);
            color: white;
        }

        .achievement-card:nth-child(2) .achievement-icon {
            background: var(--gradient-accent);
        }

        .achievement-card:nth-child(3) .achievement-icon {
            background: linear-gradient(135deg, #00CED1 0%, #20B2AA 100%);
        }
        
        .achievement-card h3 {
            font-size: 1.5rem;
            margin-bottom: 15px;
            color: var(--black);
        }
        
        .achievement-card p {
            color: var(--black);
            opacity: 0.8;
            margin-bottom: 20px;
        }
        
        .badge {
            display: inline-block;
            background: rgba(135, 206, 235, 0.2);
            color: var(--secondary);
            padding: 8px 20px;
            border-radius: 50px;
            font-size: 0.9rem;
            font-weight: 600;
            border: 1px solid rgba(135, 206, 235, 0.3);
            backdrop-filter: blur(5px);
        }
        
        /* Expertise Section with Interactive Cards */
        .expertise {
            padding: 100px 0;
            position: relative;
        }

        .expertise::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at 80% 20%, rgba(135, 206, 235, 0.1) 0%, transparent 50%);
            z-index: -1;
        }
        
        .expertise-cards {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 30px;
        }
        
        .expertise-card {
            background: rgba(255, 255, 255, 0.8);
            border-radius: 20px;
            padding: 30px;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(135, 206, 235, 0.3);
            transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            opacity: 0;
            transform: translateY(50px);
            position: relative;
            overflow: hidden;
        }

        .expertise-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 5px;
            background: var(--primary);
            transform: scaleX(0);
            transform-origin: left;
            transition: transform 0.5s ease;
        }

        .expertise-card:nth-child(2)::before {
            background: var(--accent);
        }

        .expertise-card:nth-child(3)::before {
            background: var(--secondary);
        }

        .expertise-card:hover::before {
            transform: scaleX(1);
        }

        .expertise-card.animate {
            animation: cardAppear 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards;
        }
        
        .expertise-card:hover {
            transform: translateY(-10px) scale(1.02);
            box-shadow: 0 20px 40px rgba(135, 206, 235, 0.2);
            border-color: rgba(135, 206, 235, 0.5);
            background: rgba(255, 255, 255, 0.95);
        }
        
        .expertise-header {
            display: flex;
            align-items: center;
            margin-bottom: 25px;
        }
        
        .expertise-icon {
            width: 70px;
            height: 70px;
            border-radius: 15px;
            background: var(--gradient);
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 20px;
            font-size: 1.8rem;
            color: white;
            box-shadow: 0 10px 20px rgba(135, 206, 235, 0.3);
            transition: all 0.5s ease;
            border: 1px solid rgba(135, 206, 235, 0.3);
        }

        .expertise-card:nth-child(2) .expertise-icon {
            background: var(--gradient-accent);
        }

        .expertise-card:nth-child(3) .expertise-icon {
            background: linear-gradient(135deg, #00CED1 0%, #20B2AA 100%);
        }

        .expertise-card:hover .expertise-icon {
            transform: rotate(15deg) scale(1.1);
            background: var(--gradient-accent);
        }
        
        .expertise-header h3 {
            font-size: 1.5rem;
            color: var(--black);
        }
        
        .skills-list {
            list-style-type: none;
        }
        
        .skills-list li {
            padding: 12px 0;
            border-bottom: 1px solid rgba(135, 206, 235, 0.2);
            display: flex;
            align-items: center;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .skills-list li::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            height: 100%;
            width: 5px;
            background: var(--primary);
            transform: translateX(-100%);
            transition: transform 0.3s ease;
        }

        .expertise-card:nth-child(2) .skills-list li::before {
            background: var(--accent);
        }

        .expertise-card:nth-child(3) .skills-list li::before {
            background: var(--secondary);
        }

        .skills-list li:hover {
            padding-left: 15px;
            background: rgba(135, 206, 235, 0.1);
        }

        .skills-list li:hover::before {
            transform: translateX(0);
        }
        
        .skills-list li:last-child {
            border-bottom: none;
        }
        
        .skills-list li i {
            color: var(--primary);
            margin-right: 15px;
            font-size: 1.2rem;
            transition: all 0.3s ease;
        }

        .skills-list li:hover i {
            transform: scale(1.3) rotate(5deg);
            color: var(--accent);
        }

        .skill-tag {
            background: rgba(135, 206, 235, 0.2);
            color: var(--secondary);
            padding: 4px 12px;
            border-radius: 15px;
            font-size: 0.85rem;
            margin-left: auto;
            border: 1px solid rgba(135, 206, 235, 0.3);
            transition: all 0.3s ease;
        }

        .skills-list li:hover .skill-tag {
            background: var(--primary);
            color: white;
        }

        .expertise-card:nth-child(2) .skills-list li:hover .skill-tag {
            background: var(--accent);
            color: white;
        }
        
        /* Contact Section */
        .contact {
            padding: 100px 0;
            text-align: center;
        }
        
        .contact-info {
            display: flex;
            justify-content: center;
            gap: 40px;
            margin-top: 40px;
            flex-wrap: wrap;
        }
        
        .contact-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 30px;
            background: rgba(255, 255, 255, 0.8);
            border-radius: 20px;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(135, 206, 235, 0.3);
            width: 220px;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            opacity: 0;
            transform: translateY(30px);
        }

        .contact-item.animate {
            animation: fadeUp 0.8s forwards;
        }

        .contact-item:hover {
            transform: translateY(-10px) scale(1.05);
            background: rgba(255, 255, 255, 0.95);
            border-color: var(--primary);
            box-shadow: 0 20px 40px rgba(135, 206, 235, 0.2);
        }
        
        .contact-item i {
            font-size: 2.8rem;
            margin-bottom: 20px;
            color: var(--primary);
            transition: all 0.3s ease;
        }

        .contact-item:hover i {
            transform: scale(1.2);
            color: var(--accent);
        }
        
        /* Footer */
        footer {
            background: rgba(12, 45, 72, 0.9);
            padding: 60px 0 30px;
            text-align: center;
            border-top: 1px solid rgba(135, 206, 235, 0.2);
        }
        
        .footer-name {
            font-family: 'Montserrat', sans-serif;
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 20px;
            color: var(--white);
        }
        
        .social-links {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin: 30px 0;
        }
        
        .social-links a {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 50px;
            height: 50px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            color: white;
            font-size: 1.3rem;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative;
            overflow: hidden;
            border: 1px solid rgba(135, 206, 235, 0.3);
        }

        .social-links a::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: var(--gradient);
            transition: left 0.4s ease;
            z-index: -1;
        }

        .social-links a:hover {
            transform: translateY(-8px) scale(1.1);
            color: white;
        }

        .social-links a:hover::before {
            left: 0;
        }
        
        /* Responsive Design */
        @media (max-width: 992px) {
            .hero-content {
                flex-direction: column;
                text-align: center;
            }
            
            .hero-text h1 {
                font-size: 3rem;
            }

            .profile-image-container {
                justify-content: center;
                flex-direction: column;
                text-align: center;
            }

            .profile-info {
                margin-top: 20px;
            }

            .profile-tags {
                justify-content: center;
            }
            
            .profile-container {
                width: 300px;
                height: 300px;
                margin-top: 30px;
            }

            .expertise-cards {
                grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            }
        }
        
        @media (max-width: 768px) {
            .nav-links {
                position: fixed;
                top: 80px;
                right: -100%;
                width: 70%;
                height: calc(100vh - 80px);
                background: rgba(12, 45, 72, 0.95);
                backdrop-filter: blur(20px);
                flex-direction: column;
                align-items: center;
                justify-content: flex-start;
                padding-top: 50px;
                transition: right 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
                z-index: 999;
                border-left: 1px solid rgba(135, 206, 235, 0.2);
            }

            .nav-links.active {
                right: 0;
            }
            
            .mobile-menu-btn {
                display: flex;
            }
            
            .hero-text h1 {
                font-size: 2.5rem;
            }
            
            .section-title {
                font-size: 2.2rem;
            }
            
            .achievement-cards,
            .expertise-cards {
                grid-template-columns: 1fr;
            }

            .contact-info {
                gap: 20px;
            }

            .contact-item {
                width: 100%;
                max-width: 280px;
            }
        }
        
        @media (max-width: 576px) {
            .hero {
                padding: 150px 0 60px;
            }
            
            .hero-text h1 {
                font-size: 2.2rem;
            }

            .profile-img-wrapper {
                width: 100px;
                height: 100px;
            }

            .profile-name {
                font-size: 1.5rem;
            }
            
            .profile-container {
                width: 250px;
                height: 250px;
            }
            
            .profile-front i {
                font-size: 7rem;
            }
            
            .achievements,
            .expertise,
            .contact {
                padding: 60px 0;
            }
        }
    </style>
</head>
<body>
    <!-- Animated Background -->
    <div class="bg-animation">
        <div class="bg-circle"></div>
        <div class="bg-circle"></div>
        <div class="bg-circle"></div>
    </div>

    <!-- Header & Navigation -->
    <header id="header">
        <div class="container">
            <div class="nav-container">
                <div class="logo">RUBEN L ALMODIEL</div>
                <div class="nav-links">
                    <a href="#home">Home</a>
                    <a href="#achievements">Achievements</a>
                    <a href="#expertise">Expertise</a>
                    <a href="#contact">Contact</a>
                </div>
                <div class="mobile-menu-btn">
                    <i class="fas fa-bars"></i>
                </div>
            </div>
        </div>
    </header>

    <!-- Hero Section -->
    <section id="home" class="hero">
        <div class="container">
            <div class="hero-content">
                <div class="hero-text">
                    <div class="title">IT Support</div>
                    <h1 id="typing-name"></h1>
                    
                    <!-- Profile Image Container -->
                    <div class="profile-image-container">
                        <div class="profile-img-wrapper">
                            <!-- Replace this with your actual image URL -->
                            <img src="2574cda1-b85b-49dc-b5f0-fd5b943506ff.jpg" 
                                 alt="Ruben L Almodiel" class="profile-img">
                        </div>
                        <div class="profile-info">
                            <div class="profile-name">Ruben L Almodiel</div>
                            <div class="profile-title">BSIT 3rd Year | IT Support</div>
                            <div class="profile-tags">
                                <span class="profile-tag">Developer</span>
                                <span class="profile-tag">Gamer</span>
                                <span class="profile-tag">Video Editor</span>
                            </div>
                        </div>
                    </div>
                    
                    <p>BSIT 3rd Year Student  Picture&Video Editing, and IT Support. Passionate about creating innovative solutions and achieving excellence in both technology and gaming.</p>
                    <a href="#contact" class="btn">Get In Touch</a>
                </div>
                <div class="hero-image">
                    <div class="profile-container">
                        <div class="profile-card">
                            <div class="profile-front">
                                <i class="fas fa-laptop-code"></i>
                            </div>
                            <div class="profile-back">
                                <h3>Ruben L Almodiel</h3>
                                <p>IT Support with passion for gaming and creative video editing.</p>
                                <div class="badge" style="margin-top: 15px;">Mythical Honor Player</div>
                            </div>
                        </div>
                        <div class="flip-hint">Hover to flip</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Achievements Section -->
    <section id="achievements" class="achievements">
        <div class="container">
            <h2 class="section-title">My Achievements</h2>
            <div class="achievement-cards">
                <div class="achievement-card">
                    <div class="achievement-icon">
                        <i class="fas fa-trophy"></i>
                    </div>
                    <h3>Mobile Legends</h3>
                    <p>Reached Mythical Honor rank in Mobile Legends, demonstrating strategic gameplay and team coordination skills at a competitive level.</p>
                    <div class="badge">Gaming Achievement</div>
                </div>
                
                <div class="achievement-card">
                    <div class="achievement-icon">
                        <i class="fas fa-crosshairs"></i>
                    </div>
                    <h3>Crossfire Legends</h3>
                    <p>Achieved Master 2 rank in Crossfire Legends, showcasing precision aiming, tactical awareness, and exceptional FPS skills.</p>
                    <div class="badge">Competitive Gaming</div>
                </div>
                
                <div class="achievement-card">
                    <div class="achievement-icon">
                        <i class="fas fa-brain"></i>
                    </div>
                    <h3>Zombie Tsunami</h3>
                    <p>Reached 140 brains high score in Zombie Tsunami, demonstrating strategic thinking and quick decision-making under pressure.</p>
                    <div class="badge">Strategy Gaming</div>
                </div>
                
                <div class="achievement-card">
                    <div class="achievement-icon">
                        <i class="fas fa-graduation-cap"></i>
                    </div>
                    <h3>BSIT 3rd Year</h3>
                    <p>Currently in my 3rd year of Bachelor of Science in Information Technology, maintaining academic excellence while developing practical IT skills.</p>
                    <div class="badge">Academic Achievement</div>
                </div>
            </div>
        </div>
    </section>

    <!-- Expertise Section -->
    <section id="expertise" class="expertise">
        <div class="container">
            <h2 class="section-title">My Expertise</h2>
            <div class="expertise-cards">
                <div class="expertise-card">
                    <div class="expertise-header">
                        <div class="expertise-icon">
                            <i class="fas fa-headset"></i>
                        </div>
                        <h3>IT Support</h3>
                    </div>
                    <ul class="skills-list">
                        <li><i class="fas fa-check-circle"></i> Troubleshooting <span class="skill-tag">Expert</span></li>
                        <li><i class="fas fa-check-circle"></i> Hardware Maintenance <span class="skill-tag">Advanced</span></li>
                        <li><i class="fas fa-check-circle"></i> Software Installation <span class="skill-tag">Expert</span></li>
                        <li><i class="fas fa-check-circle"></i> Network Configuration <span class="skill-tag">Intermediate</span></li>
                        <li><i class="fas fa-check-circle"></i> System Administration <span class="skill-tag">Intermediate</span></li>
                        <li><i class="fas fa-check-circle"></i> Technical Support <span class="skill-tag">Expert</span></li>
                    </ul>
                </div>
                
                <div class="expertise-card">
                    <div class="expertise-header">
                        <div class="expertise-icon">
                            <i class="fas fa-video"></i>
                        </div>
                        <h3>Picture & Video Editing</h3>
                    </div>
                    <ul class="skills-list">
                        <li><i class="fas fa-check-circle"></i> Capcut <span class="skill-tag">Expert</span></li>
                        <li><i class="fas fa-check-circle"></i> Filmora <span class="skill-tag">Advanced</span></li>
                        <li><i class="fas fa-check-circle"></i> Adobe Photoshop <span class="skill-tag">Intermediate</span></li>
                        <li><i class="fas fa-check-circle"></i> Adobe Premiere Pro <span class="skill-tag">Intermediate</span></li>
                        <li><i class="fas fa-check-circle"></i> Color Correction <span class="skill-tag">Advanced</span></li>
                        <li><i class="fas fa-check-circle"></i> Photo Editing <span class="skill-tag">Advanced</span></li>
                    </ul>
                </div>
                
                <div class="expertise-card">
                    <div class="expertise-header">
                        <div class="expertise-icon">
                            <i class="fas fa-paint-brush"></i>
                        </div>
                        <h3>Designing</h3>
                    </div>
                    <ul class="skills-list">
                        <li><i class="fas fa-check-circle"></i> UI/UX Design <span class="skill-tag">Intermediate</span></li>
                        <li><i class="fas fa-check-circle"></i> Graphic Design <span class="skill-tag">Intermediate</span></li>
                        <li><i class="fas fa-check-circle"></i> Web Design <span class="skill-tag">Intermediate</span></li>
                        <li><i class="fas fa-check-circle"></i> Layout Design <span class="skill-tag">Advanced</span></li>
                        <li><i class="fas fa-check-circle"></i> Typography <span class="skill-tag">Intermediate</span></li>
                        <li><i class="fas fa-check-circle"></i> Color Theory <span class="skill-tag">Intermediate</span></li>
                    </ul>
                </div>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section id="contact" class="contact">
        <div class="container">
            <h2 class="section-title">Get In Touch</h2>
            <p style="color: var(--black); opacity: 0.8; max-width: 700px; margin: 0 auto 40px;">Feel free to reach out for collaboration, job opportunities, or just to talk about tech, gaming, and video editing!</p>
            
            <div class="contact-info">
                <div class="contact-item">
                    <i class="fas fa-envelope"></i>
                    <h4>Email</h4>
                    <p>rubenalmodiel2@gmail.com</p>
                </div>
                
                <div class="contact-item">
                    <i class="fas fa-phone"></i>
                    <h4>Phone</h4>
                    <p>639127205683</p>
                </div>
                
                <div class="contact-item">
                    <i class="fas fa-map-marker-alt"></i>
                    <h4>Location</h4>
                    <p>Santa fe Cebu Philippines</p>
                </div>
            </div>
            
            <a href="mailto:rubenalmodiel2@gmail.com" class="btn" style="margin-top: 50px;">Send Message</a>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="footer-name">Ruben L Almodiel</div>
            <p style="color: rgba(255, 255, 255, 0.8); max-width: 600px; margin: 0 auto;">IT Specialist with expertise in IT support, video editing, and design</p>
            
            <div class="social-links">
                <a href="#"><i class="fab fa-linkedin-in"></i></a>
                <a href="#"><i class="fab fa-github"></i></a>
                <a href="#"><i class="fab fa-facebook-f"></i></a>
                <a href="#"><i class="fab fa-youtube"></i></a>
                <a href="#"><i class="fab fa-instagram"></i></a>
            </div>
            
            <p style="color: rgba(255, 255, 255, 0.6); margin-top: 30px;">&copy; 2023 Ruben L Almodiel. All Rights Reserved.</p>
        </div>
    </footer>

    <script>
        // Mobile menu toggle
        const mobileMenuBtn = document.querySelector('.mobile-menu-btn');
        const navLinks = document.querySelector('.nav-links');
        
        mobileMenuBtn.addEventListener('click', () => {
            navLinks.classList.toggle('active');
            mobileMenuBtn.querySelector('i').classList.toggle('fa-bars');
            mobileMenuBtn.querySelector('i').classList.toggle('fa-times');
        });
        
        // Header scroll effect
        const header = document.getElementById('header');
        window.addEventListener('scroll', () => {
            if (window.scrollY > 100) {
                header.classList.add('scrolled');
            } else {
                header.classList.remove('scrolled');
            }
        });
        
        // Smooth scrolling for navigation links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function(e) {
                e.preventDefault();
                
                const targetId = this.getAttribute('href');
                if(targetId === '#') return;
                
                const targetElement = document.querySelector(targetId);
                if(targetElement) {
                    window.scrollTo({
                        top: targetElement.offsetTop - 80,
                        behavior: 'smooth'
                    });
                    
                    // Close mobile menu if open
                    if(window.innerWidth <= 768) {
                        navLinks.classList.remove('active');
                        mobileMenuBtn.querySelector('i').classList.remove('fa-times');
                        mobileMenuBtn.querySelector('i').classList.add('fa-bars');
                    }
                }
            });
        });
        
        // Intersection Observer for animations
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };
        
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if(entry.isIntersecting) {
                    entry.target.classList.add('animate');
                    
                    // For contact items - stagger animation
                    if(entry.target.classList.contains('contact-item')) {
                        const delay = Array.from(document.querySelectorAll('.contact-item')).indexOf(entry.target) * 0.2;
                        entry.target.style.animationDelay = `${delay}s`;
                    }
                    
                    // For achievement and expertise cards
                    if(entry.target.classList.contains('achievement-card') || 
                       entry.target.classList.contains('expertise-card')) {
                        const cards = document.querySelectorAll('.achievement-card, .expertise-card');
                        const index = Array.from(cards).indexOf(entry.target);
                        const delay = index * 0.1;
                        entry.target.style.animationDelay = `${delay}s`;
                    }
                }
            });
        }, observerOptions);
        
        // Observe elements for animation
        document.querySelectorAll('.achievement-card, .expertise-card, .contact-item').forEach(element => {
            observer.observe(element);
        });

        // Observe section titles
        document.querySelectorAll('.section-title').forEach(title => {
            observer.observe(title);
        });
        
        // Parallax effect on mouse move
        document.addEventListener('mousemove', (e) => {
            const moveX = (e.clientX - window.innerWidth / 2) * 0.01;
            const moveY = (e.clientY - window.innerHeight / 2) * 0.01;
            
            document.querySelector('.hero').style.transform = `translate(${moveX}px, ${moveY}px)`;
        });

        // Typing effect for name
        const nameElement = document.getElementById('typing-name');
        const fullName = "Ruben L Almodiel";
        let nameText = "";
        let nameIndex = 0;
        
        function typeName() {
            if (nameIndex < fullName.length) {
                nameText += fullName.charAt(nameIndex);
                nameElement.textContent = nameText;
                nameIndex++;
                setTimeout(typeName, 100);
            }
        }
        
        // Start typing animation after page loads
        window.addEventListener('load', () => {
            nameElement.textContent = "";
            setTimeout(typeName, 500);
        });

        // Add glowing effect to badges on hover
        document.querySelectorAll('.badge, .profile-tag').forEach(badge => {
            badge.addEventListener('mouseenter', function() {
                this.style.boxShadow = '0 0 20px rgba(135, 206, 235, 0.5)';
                this.style.transform = 'scale(1.05)';
            });
            
            badge.addEventListener('mouseleave', function() {
                this.style.boxShadow = 'none';
                this.style.transform = 'scale(1)';
            });
        });

        // Add particle effect to buttons on click
        document.querySelectorAll('.btn').forEach(button => {
            button.addEventListener('click', function(e) {
                // Create particles
                for(let i = 0; i < 10; i++) {
                    const particle = document.createElement('div');
                    particle.style.position = 'absolute';
                    particle.style.width = '8px';
                    particle.style.height = '8px';
                    particle.style.backgroundColor = '#87CEEB';
                    particle.style.borderRadius = '50%';
                    particle.style.pointerEvents = 'none';
                    
                    const rect = this.getBoundingClientRect();
                    particle.style.left = `${e.clientX - rect.left}px`;
                    particle.style.top = `${e.clientY - rect.top}px`;
                    
                    this.appendChild(particle);
                    
                    // Animate particle
                    const angle = Math.random() * Math.PI * 2;
                    const velocity = 2 + Math.random() * 3;
                    const vx = Math.cos(angle) * velocity;
                    const vy = Math.sin(angle) * velocity;
                    
                    let opacity = 1;
                    const animateParticle = () => {
                        opacity -= 0.05;
                        particle.style.opacity = opacity;
                        particle.style.transform = `translate(${vx * 10}px, ${vy * 10}px)`;
                        
                        if(opacity > 0) {
                            requestAnimationFrame(animateParticle);
                        } else {
                            particle.remove();
                        }
                    };
                    
                    requestAnimationFrame(animateParticle);
                }
            });
        });

        // Image upload simulation (for demo purposes)
        // In a real application, you would replace the image source with your actual image
        document.querySelector('.profile-img-wrapper').addEventListener('click', function() {
            // This is a demo - in a real app you would implement file upload
            alert('Click to upload your own profile image! In a real application, this would open a file picker.');
        });
    </script>
</body>
</html>







